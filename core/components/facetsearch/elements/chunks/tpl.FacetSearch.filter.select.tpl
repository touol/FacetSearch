<div class="facetsearch-filter facetsearch-filter-select facetsearch-{$alias}">
  <h4 class="filter-title">{$label}</h4>
  <select name="{$alias}" class="form-control">
		<option value="" {$selected}>Выберите из списка</option>
    {$rows}
  </select>
</div>